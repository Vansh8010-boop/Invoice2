{
  "name": "writingale-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Serverless backend for writingale — stores all invoice data in a real .xlsx workbook via Vercel Blob storage.",
  "scripts": {
    "dev": "vercel dev"
  },
  "dependencies": {
    "@vercel/blob": "^1.0.0",
    "xlsx": "^0.18.5"
  }
}
